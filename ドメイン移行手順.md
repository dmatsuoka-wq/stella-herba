# 独自ドメイン（stellaherba.com）移行手順

作成日: 2026-08-13

## 完了済み（ファイル側の変更）

以下のファイルを新ドメイン `https://stellaherba.com/` に書き換え済みです。GitHubに上書きアップロードしてください。

| ファイル | 変更内容 |
|---|---|
| index.html | canonical / og:url / og:image / twitter:image / 構造化データの@id・url・publisher参照 |
| yomogimushi-guide.html | canonical / og:url / og:image / Article・BreadcrumbListのURL |
| sango-yomogimushi.html | canonical / og:url / og:image / Article・BreadcrumbListのURL |
| sitemap.xml | 3ページすべてのURL |
| robots.txt | サイトマップのURL |
| llms.txt | 各ページのURL |
| vercel.json（新規） | 画像キャッシュ設定、.html不要のURL対応 |

### 【重要】vercel.json のリダイレクト設定について
当初 vercel.json に「旧 vercel.app → 新ドメイン」の転送設定を入れていましたが、**DNS反映が完了するまでは旧URLも表示できなくなる**ため削除しました。www→wwwなしの転送は Vercel の Domains 画面側（308設定）で行っているため、vercel.json 側の記述は不要です。

新ドメインが正常に表示されるようになった後、旧 vercel.app URL を新ドメインへ転送したい場合は、Vercel の Domains 画面で `stella-herba.vercel.app` の「編集」→「別のドメインにリダイレクトする」→ `stellaherba.com`（308）に変更するのが安全です（vercel.json を編集する必要はありません）。

**方針**: `www` なしの `https://stellaherba.com` を正規URLとしました。`www.stellaherba.com` でアクセスされた場合は自動で `www` なしへ転送されます。

---

## 作業手順

### 1. GitHubにファイルをアップロード
上記7ファイルをリポジトリのルート直下に上書きアップロードします（vercel.json も同じ場所です）。

### 2. Vercelにドメインを追加
1. Vercelダッシュボード → 該当プロジェクト → **Settings → Domains**
2. `stellaherba.com` を入力して Add
3. 続けて `www.stellaherba.com` も追加（リダイレクト用。Vercelが自動でリダイレクト設定を提案する場合はそれに従ってOK）
4. 画面に表示されるDNS設定内容をメモします

### 3. DNSを設定（ドメインを購入した事業者の管理画面）
Vercelの指示に従って、いずれかを設定します。

**パターンA：ネームサーバーを変更する場合**
Vercelが指定するネームサーバー（例: ns1.vercel-dns.com / ns2.vercel-dns.com）に変更します。設定は1回で済み、以後の管理が楽です。

**パターンB：レコードを個別に設定する場合**
- `stellaherba.com` → A レコード → `76.76.21.21`
- `www` → CNAME レコード → `cname.vercel-dns.com`

※上記IPはVercelの画面に表示された値を必ず確認してください（変更される可能性があります）。

反映には数分〜最大48時間かかります。Vercelの Domains 画面で「Valid Configuration」と表示されれば完了です。SSL証明書（https）はVercelが自動発行します。

### 4. 表示確認
以下すべてが正しく表示・転送されるか確認します。

- https://stellaherba.com/ → 表示される
- https://stellaherba.com/yomogimushi-guide.html → 表示される
- https://stellaherba.com/sango-yomogimushi.html → 表示される
- https://www.stellaherba.com/ → wwwなしに転送される
- https://stella-herba.vercel.app/ → 新ドメインに転送される

### 5. Google Search Console の移行（重要）
1. Search Console で新しいプロパティ `https://stellaherba.com` を追加（DNS認証またはHTMLタグ認証）
2. 新プロパティで **サイトマップを送信**：`https://stellaherba.com/sitemap.xml`
3. 旧プロパティ（stella-herba.vercel.app）の設定 → **アドレス変更ツール** で新ドメインへの移行を申告
   - これにより旧URLの検索評価が新ドメインに引き継がれます
4. 各ページで「URL検査」→「インデックス登録をリクエスト」を実行すると反映が早まります

※index.html 内の `google-site-verification` メタタグはそのまま残してあります。新プロパティでHTMLタグ認証を使う場合、新しいコードが発行されたら差し替えてください（DNS認証を選べば不要です）。

### 6. 外部サービスのURL更新
サイトのURLを掲載している箇所をすべて新ドメインに変更します。

- [ ] Googleビジネスプロフィール（ウェブサイト欄）
- [ ] Instagram プロフィールのリンク
- [ ] note のプロフィール・記事内リンク
- [ ] 公式LINEのリッチメニュー・あいさつメッセージ
- [ ] 名刺・チラシ・ショップカード（次回作成分から）

---

## 補足

### 旧ドメインはいつまで残す？
`stella-herba.vercel.app` はVercelプロジェクトに紐づく無料URLなので、削除せずそのまま残しておいて問題ありません。vercel.json のリダイレクト設定により、旧URLへのアクセスは自動的に新ドメインへ転送されます。

### 検索順位への影響
301リダイレクトとSearch Consoleのアドレス変更を正しく行えば、評価はほぼそのまま引き継がれます。移行直後の1〜2週間は順位が多少揺れることがありますが、通常は元に戻ります。焦って何度も設定を変えないことが大切です。

### メールアドレスも作れます
独自ドメインを取得したので `info@stellaherba.com` のようなメールアドレスも作成できます（Google Workspace 等の別途契約が必要）。サロンの信頼感が上がるので、余力があればご検討ください。

### 今後のvercel.jsonの利点
`"cleanUrls": true` を設定したので、今後は `https://stellaherba.com/yomogimushi-guide` のように `.html` なしのURLでもアクセスできるようになります。将来ページを増やすときは、こちらの短いURLで案内するとスマートです（既存の.html付きURLも引き続き有効です）。
